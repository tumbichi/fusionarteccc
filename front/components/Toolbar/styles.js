import styled from 'styled-components';

export const ToolbarWrapper = styled.div.attrs({
  className: 'bg-green-primary flex justify-between items-center h-12 md:h-16 lg:h-20',
})``;

export const ImageWrapper = styled.div.attrs({
  className: '',
})``;

/*  background-image: url(${({ image }) => image});*/
